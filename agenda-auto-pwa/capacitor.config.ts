import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'agenda-auto-pwa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
